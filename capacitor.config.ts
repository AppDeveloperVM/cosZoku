import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vercel.coszoku',
  appName: 'cosZoku',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
